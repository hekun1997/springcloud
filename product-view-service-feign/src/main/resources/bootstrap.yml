spring:
  cloud:
    config:
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
    bus:
      enabled: true
      trace:
        enabled: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


rabbitmq:
  host: 192.168.224.11
  port: 5672
  username: guest
  password: guest